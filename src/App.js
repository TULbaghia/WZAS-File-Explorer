import LoadDir from "./components/LoadDir";

function App() {
    return (
        <div className="App">
            <header className={"App-header"}>Header</header>
            <main className={"App-main"}><LoadDir/></main>
            <footer className={"App-footer"}>Footer</footer>
        </div>
    );
}

export default App;
